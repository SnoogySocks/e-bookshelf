import styled from "styled-components";

export const Wrapper = styled.div`
	display: flex;
    position: absolute;
    height: 95vh;
    background: black;
	padding: 40px 20px;
`;

export const Content = styled.div`

	h1 {
        color: var(--lightGrey);
    }
`;